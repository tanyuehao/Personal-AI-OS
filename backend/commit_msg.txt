P0: rewrite auth rotation tests - 11 tests covering full lifecycle

Tests:
1. test_rotation_t1_to_t2: T1->T2 rotation, T1 marked used, T1!=T2
2. test_replay_revokes_family: T1 replay revokes entire token family
3. test_t2_dead_after_replay: T2 must be 401 after T1 replay
4. test_logout_invalidates_token: logout revokes refresh token
5. test_logout_then_different_login_works: logout then fresh login works
6. test_multiple_logins_independent: 3 logins produce 3 independent tokens
7. test_jwt_jti_matches_db_jti: JWT.jti === DB.jti verified
8. test_rotation_new_jti_matches_db: rotated token JWT.jti === DB.jti
9. test_malformed_token_rejected: invalid JWT returns 401
10. test_empty_token_rejected: empty string returns 400
11. test_access_token_as_refresh_rejected: access token cannot be used as refresh

Also: replay detection now revokes the replayed token itself (is_revoked=True).
